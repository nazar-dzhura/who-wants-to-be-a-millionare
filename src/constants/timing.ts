/** Delay before revealing whether the selected answer is correct/wrong */
export const REVEAL_DELAY_MS = 1000;

/** Delay after reveal before advancing to next question or ending the game */
export const ADVANCE_DELAY_MS = 1500;
